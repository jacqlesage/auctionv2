function updatePhoneNumber() {

    //check if it has changed and not empty

    //if empty then no change / ignore the click in the box basically

    //if yes then make the change on the DB via ajax using controllers


    
}